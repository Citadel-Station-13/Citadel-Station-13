diff a/code/datums/forced_movement.dm b/code/datums/forced_movement.dm	(rejected hunks)
@@ -82,9 +82,9 @@
 
 	. = . && (vic.loc != tar.loc)
 
-/mob/Bump(atom/A)
+/mob/Collide(atom/A)
 	. = ..()
-	if(force_moving && force_moving.allow_climbing && istype(A,/obj/structure))
+	if(force_moving && force_moving.allow_climbing && istype(A, /obj/structure))
 		var/obj/structure/S = A
 		if(S.climbable)
 			S.do_climb(src)
