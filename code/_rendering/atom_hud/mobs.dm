/**
 * Initialize HUds
 *
 * Call during Initialize()
 */
/mob/proc/InitializeHUDs()

/**
 * Add HUD
 */
/mob/proc/AddHUD(id, source = HUD_SOURCE_ADMINBUS)

/**
 * Remove HUD
 */
/mob/proc/RemoveHUD(id, source)

/**
 * Reapply HUD images, usually done on login
 */
/mob/proc/ReloadHUD()
