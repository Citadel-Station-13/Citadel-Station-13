diff a/code/modules/projectiles/ammunition.dm b/code/modules/projectiles/ammunition.dm	(rejected hunks)
@@ -53,8 +53,8 @@
 					continue
 			if (boolets > 0)
 				box.update_icon()
-				user << "<span class='notice'>You collect [boolets] shell\s. [box] now contains [box.stored_ammo.len] shell\s.</span>"
+				to_chat(user, "<span class='notice'>You collect [boolets] shell\s. [box] now contains [box.stored_ammo.len] shell\s.</span>")
 			else
-				user << "<span class='warning'>You fail to collect anything!</span>"
+				to_chat(user, "<span class='warning'>You fail to collect anything!</span>")
 	else
 		return ..()
